
@{
    ViewBag.Title = "Index";
}


</div>
<h2 class="display-4">Employee Information</h2>
<div class="d-flex justify-content-end">
    <a href="@Url.Action("Create","Employee")" class="btn btn-primary"> <i class="fa fa-plus-circle"></i>Add New Emoloyee</a>
</div>
<div>
    <a href="@Url.Action("Aggrigate","Employee")" class="btn btn-primary btn-sm">Aggregate</a>
</div>
<div id="table-div">
    @Html.Action("ShowIndex")
</div>

@section styles{
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link href="~/Content/PagedList.css" rel="stylesheet" />
}
@section scripts {
    <script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>

    <script>
        $(document).on("click", "button.show", function () {
            var showBtn = $(this);
            var hideBtn = showBtn.next();
            var trSh = showBtn.closest("tr");
            var trSt = trSh.next();
            showBtn.hide();
            hideBtn.show();
            trSt.show();
        });
        $(document).on("click", "button.hide", function () {
            var hideBtn = $(this);
            var showBtn = hideBtn.prev();
            var trSh = showBtn.closest("tr");
            var trSt = trSh.next();
            showBtn.show();
            hideBtn.hide();
            trSt.hide();
        });
    </script>
}
